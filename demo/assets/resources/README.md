# Resources

Use this folder for Cocos `resources` assets that need runtime loading by key. The first Cocos implementation uses procedural `Graphics` rendering and does not require runtime-loaded art assets.
