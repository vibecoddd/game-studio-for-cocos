# Prefabs

Use this folder for Cocos Creator prefabs as the demo grows:

- player ship view
- drone sentinel view
- shard pickup view
- HUD panels
- transient feedback effects

The current demo draws simple gameplay shapes through a Cocos `Graphics` component so it can keep the first Cocos implementation small and inspectable.
